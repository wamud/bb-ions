The 'interleaved' syndrome extraction circuits are from relayBP github (in tests/.../bicycle_bivariate) and are as far as I can tell the syndrome extraction circuit from the original BB paper. (RelayBP and original BB paper both released by IBM so that would make sense). 'Non-interleaved' are using my X then Z syndrome extraction. Both are uniform noise (no shuttling errors). So basically I am comparing non-interleaved to interleaved. 